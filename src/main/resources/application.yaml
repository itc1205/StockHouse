spring:
  application:
    name: "StockHouse"
  profiles:
    active: @spring.profiles.active@
  batch:
    job:
      enabled: false
springdoc:
  api-docs:
    path: "/api-docs"

app:
  scheduling:
    mode: batch # none, simple, batch
    rate: 600000 #ms
  output_file: test.txt
  priceIncreasePercentage: 12.5