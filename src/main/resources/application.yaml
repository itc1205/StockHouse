spring:
  application:
    name: "StockHouse"
  profiles:
    active: @spring.profiles.active@
  liquibase:
    change-log: classpath:db/changelog/changelog.xml
springdoc:
  api-docs:
    path: "/api-docs"

app:
  scheduling:
    enabled: true
    optimization: false
    rate: 1000 #ms